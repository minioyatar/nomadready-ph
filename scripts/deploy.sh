#!/bin/bash
# deploy.sh — deploys to production
# Full implementation will be completed in feature/cicd-deployment
# WARNING: This script is a placeholder. Do not run until deployment is configured.

echo "NomadReady PH — deploy.sh"
echo "This script is a placeholder. Do not run in production yet."
echo ""
echo "Planned steps:"
echo "  1. Pull latest from main"
echo "  2. docker compose -f docker-compose.prod.yml build"
echo "  3. docker compose -f docker-compose.prod.yml up -d"
echo "  4. Run migrations"
echo "  5. Verify health endpoint"
