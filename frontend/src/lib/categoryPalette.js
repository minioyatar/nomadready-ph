// Re-exported from constants.js — single source of truth for shared constants.
// Kept as a shim so existing imports of CATEGORY_PALETTE / CATEGORY_COLORS
// from this path continue to work unchanged.
export { CATEGORY_PALETTE, CATEGORY_COLORS } from './constants';