# Placeholder — views will be implemented in feature/scoring-engine
from django.http import JsonResponse


def placeholder(request):
    return JsonResponse({"detail": "scoring app — not yet implemented"})
